chennai_customers = customers.filter(customers["city"] == "Chennai")
age_above_25 = customers.filter(customers["age"] > 25)

chennai_customers.show()
age_above_25.show()

# Selecting columns
selected_data = customers.select("customer_name", "city")
selected_data.show()

# Grouping
city_counts = customers.groupBy("city").count()
city_counts.show()
